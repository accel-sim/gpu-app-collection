#!/bin/bash
# Wrapper for Newton robot cartpole benchmark

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/setup_newton.sh"

# Run Newton example
python -m newton.examples.robot.example_robot_cartpole --viewer null --num-frames 5
